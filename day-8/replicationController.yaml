apiVersion: v1
kind: ReplicationController
metadata:
  name: nginx-rc
  env: demo
spec:
  template:
    metadata:
      name: nginx-pod
      labels:
        env: demo
        type: frontend
    spec:
      containers:
        - name: nginx-container
          image: nginx123
          ports:
            - containerPort: 80

